## TIER IV universe / split mirror (sensing & perception)

This repository split mirrors `main` branch of `autowarefoundation/autoware_universe`, `sensing/` and `perception/` directories, into `awf-latest` branch, used for TIER IV release workflows.

This branch only holds workflow files. See other branches for source codes.
